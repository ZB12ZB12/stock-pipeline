services:
  stock-pipeline:
    build: .
    container_name: stock-pipeline

    env_file:
      - .env # 給 container 讀設定

    volumes:
      - ./data:/app/data # 掛進 container，SQLite 才會保留
      - ./credentials:/app/credentials # 掛進 container，Google 金鑰才讀得到

    restart: "no"